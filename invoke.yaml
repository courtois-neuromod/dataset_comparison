# invoke.yaml

# Code folder
code_dir: analysis

# Notebooks folder
notebooks_dir: notebooks

# Source data folder (manually curated YAML + MD files)
source_data_dir: source_data

# Output data folder
output_data_dir: output_data
